name: PR — Appointment Service

on:
  pull_request:
    branches: [main]
    paths:
      - "services/appointment/**"

jobs:
  build:
    name: Build e Formatação
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configurar .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.x"

      - name: Restaurar dependências
        run: dotnet restore services/appointment/AppointmentService.slnx

      - name: Build
        run: dotnet build services/appointment/AppointmentService.slnx --no-restore --configuration Release

      - name: Verificar formatação
        run: dotnet format services/appointment/AppointmentService.slnx whitespace --verify-no-changes --no-restore

  test:
    name: Testes
    runs-on: ubuntu-latest
    needs: build
    if: hashFiles('services/appointment/**/*.Tests.csproj') != ''

    steps:
      - uses: actions/checkout@v4

      - name: Configurar .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.x"

      - name: Restaurar dependências
        run: dotnet restore services/appointment/AppointmentService.slnx

      - name: Executar testes
        run: dotnet test services/appointment/AppointmentService.slnx --no-restore --configuration Release
